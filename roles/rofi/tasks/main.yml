---
- name: Rofi | Checking for Distribution Config | {{ ansible_facts.distribution }}
  ansible.builtin.stat:
    path: "{{ role_path }}/tasks/{{ ansible_facts.distribution }}.yml"
  register: rofi_distribution_config

- name: Rofi | Run Tasks | {{ ansible_facts.distribution }}
  ansible.builtin.include_tasks: "{{ ansible_facts.distribution }}.yml"
  when: rofi_distribution_config.stat.exists

- name: Rofi | Copy config
  ansible.builtin.copy:
    src: "{{ role_path }}/files/rofi"
    dest: "{{ ansible_facts.user_dir }}/.config/"
    mode: '644'
    directory_mode: '755'
